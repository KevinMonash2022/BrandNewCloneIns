/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains:["jennexplores.com", "upload.wikimedia.org", "lh3.googleusercontent.com"]
  }
  
};

export default nextConfig;
